# Arguments, Basic Syntax, Strings, Reading the Docs
# This level teachs the user to use external resources when stuck 
hero.say("What's the password?")
# Use the "say()" function to say the password.
# The password is: "Achoo"
hero.say("Achoo")
hero.moveUp(2)
